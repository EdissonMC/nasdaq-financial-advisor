services:
  logging_service:
    build: .
    container_name: logging_service
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data   # persist SQLite DB
    environment:
      - LOGGING_DB_PATH=/data/interactions.db
